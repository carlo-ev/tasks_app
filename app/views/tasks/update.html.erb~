<h1>Edit your quote! </h1>
<%= form_for @task do |f| %>
	<p>
		<%= f.label :description %>
		<%= f.text_area :description%>
		<%= @task.description  %>
	</p>
	<p>
		<%= f.label :priority %>
		<%= f.number_field :priority %>
		<%=  @task.priority %>
	</p>
	<p>  <%= f.submit %></p>
<% end %>
